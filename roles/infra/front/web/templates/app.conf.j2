server {
    listen 80;
    server_name {{ common_name }};

    root /var/www/html;

    location / {
        rewrite ^(.*) https://{{ common_name }}$1 permanent;
    }

    include /etc/nginx/conf.d/acme.inc;
}
